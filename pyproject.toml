[project]
name = "immich-telegram-bot"
version = "0.1.0"
description = "Telegram bot that uploads forwarded photos, videos and archives to an Immich instance."
readme = "README.md"
requires-python = ">=3.12"
license = { text = "AGPL-3.0-only" }
authors = [{ name = "gigavat" }]
keywords = ["immich", "telegram", "bot", "self-hosted", "photos"]
classifiers = [
    "Programming Language :: Python :: 3.12",
    "License :: OSI Approved :: GNU Affero General Public License v3",
    "Operating System :: POSIX :: Linux",
    "Topic :: Communications :: Chat",
    "Topic :: Multimedia :: Graphics",
]
dependencies = [
    "aiogram>=3.4,<4",
    "httpx>=0.27",
    "aiosqlite>=0.20",
    "pydantic>=2.6",
    "pydantic-settings>=2.2",
    "Pillow>=10.2",
]

[project.optional-dependencies]
dev = [
    "ruff>=0.4",
    "mypy>=1.9",
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
]

[project.urls]
Homepage = "https://github.com/gigavat/immich-telegram-bot"
Issues = "https://github.com/gigavat/immich-telegram-bot/issues"

[project.scripts]
immich-telegram-bot = "immich_tg_bot.__main__:main"

[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B", "ASYNC"]
# ASYNC240: We make occasional pathlib calls inside async handlers
# (mkdir, exists, stat). For a single-user Telegram bot they run on local
# filesystem in microseconds and don't warrant the noise of asyncio.to_thread
# wrappers. Revisit if/when we add high-concurrency workloads.
ignore = ["ASYNC240"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
